{% extends 'base.html.twig' %}

{% block title %}Modifier une sortie{% endblock %}

{% block body %}
    <main id="modifier_sortie">
        <div class="container-fluid mx-0 px-0">
            <div class="forms mx-0 px-0 my-0" id="modifier_sortie_block">
                <h1>Edit Sortie</h1>

                {{ include('sortie/_form.html.twig', {'button_label': 'Update'}) }}

                {#        {% for part in participant %}#}
                {#            <p>{{ part.nom }} {{ part.prenom }}</p>#}
                {#        {% endfor %}#}

                <a href="{{ path('app_sortie_index') }}">back to list</a>

                {{ include('sortie/_delete_form.html.twig') }}
            </div>
        </div>

    </main>

{% endblock %}