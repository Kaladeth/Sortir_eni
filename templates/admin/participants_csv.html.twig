<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-iYQeCzEYFbKjA/T2uDLTpkwGzCiq6soy8tYaI1GyVh/UjpbCx/TYkiZhlZB6+fzT" crossorigin="anonymous">
    <link rel="stylesheet" href="{{ asset('css/style.css') }}">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&family=Varela+Round&display=swap" rel="stylesheet">
    <script src="https://kit.fontawesome.com/834037d968.js" crossorigin="anonymous"></script>
    <title>{% block title %}ENI - CSV File{% endblock %}</title>
</head>
    <body style="background: #6c757d">
        <br>
        <div class="container mt-5">
            <div class="d-flex justify-content-center mt-5">
                <h3>Ajouter des participants par intégration d'un fichier CSV</h3>
            </div>

            <div class="mt-5">
                 {{ form_start(form) }}
                <div class="row">
                    <div class="col-6">
                        {{ form_row(form.csvfile) }}
                    </div>
                    <div class="col-6">
                        {{ form_row(form.save) }}
                    </div>
                </div>
                {{ form_end (form) }}
            </div>

        </div>

        <div class="container mt-5">
            <div class="d-flex justify-content-center mt-5">
                <a href="{{ path('admin') }}" id="panneau_admin"><i class="fa-solid fa-screwdriver-wrench mx-2"></i> <span style="color: #0f172a">Retour panneau administrateur</span></a>
            </div>
        </div>
    </body>
</html>
