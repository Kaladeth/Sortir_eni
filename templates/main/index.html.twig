{% extends 'base.html.twig' %}

{% block title %}{{parent()}}Accueil{% endblock %}

{% block body %}
    <div class="container-fluid text-center" id="accueil">
        <h1 class="pt-5">Bienvenue dans l'espace SORTIES de l'ENI !</h1>
        <p>Rien ne vaut une bonne sortie pour se sentir mieux après une dure semaine d'apprentissage !</p>
        <div class="container">
            <div class="row">
                {% for sortie in sorties %}
                    <div class="col-4">
                        <div class="card" style="width: 100%;">
                            <img src="img/uploads/{{ sortie.imageName }}" class="card-img-top" alt="...">
                            <div class="card-body">
                                <h5 class="card-title">{{ sortie.nom }}</h5>
                                <p class="card-text">{{ sortie.infosSortie }}</p>
                                <a href="#" class="btn btn-primary">Go somewhere</a>
                            </div>
                        </div>
                    </div>
{#                    <img src= alt="">#}
                {% endfor %}
            </div>

        </div>

    </div>
{% endblock %}